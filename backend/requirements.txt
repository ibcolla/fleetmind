fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-dotenv==1.0.1

# LangChain
langchain==0.3.0
langchain-core==0.3.0
langchain-openai==0.2.0
langchain-community==0.3.0

# Nebius (OpenAI-compatible, no extra package needed)

# Search (Keyless DuckDuckGo)
duckduckgo-search>=6.0.0


# mem0
mem0ai==0.1.29

# Composio
composio-langchain==0.5.40

# Utils
httpx==0.27.2
asyncio==3.4.3

# Authentication — Phase 1
# python-jose: JWT decoding/verification (HS256 for Supabase JWTs)
python-jose[cryptography]==3.3.0

# httpx is already listed above; needed for TestClient in tests
# Database — Phase 2
supabase>=2.0.0

# Billing — Phase 4
stripe>=10.0.0

# LLM Resilience — Phase 5
tenacity>=8.2.0

# Production Server — Phase 6
gunicorn>=21.2.0

# pytest stack
pytest==8.3.2
pytest-asyncio==0.23.8

